model LocationNames {
  id String @id @default(cuid())

  fr String
  en String

  location Location?

  @@unique([fr, en])
}

model Location {
  id String @id @default(cuid())

  bosses Boss[]

  names           LocationNames @relation(fields: [locationNamesId], references: [id])
  locationNamesId String        @unique
}
